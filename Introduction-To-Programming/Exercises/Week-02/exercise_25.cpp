// Напишете програма, която въвежда две 
// неотрицателни числа и изписва по-голямото от тях. 
// (А можете ли без да използвате сравнение?)
#include <iostream>

int main() {
    int a, b = 0;
    std::cin >> a >> b;
    std::cout << (a + b + std::abs(a - b)) / 2 << std::endl;
    return 0;
}
